# quality-standards.md（非機能要件・品質目標）

> ステータス: レビュー完了・要件定義確定（Step1範囲）（全9特性記載済み）

ISO/IEC 25010:2023 に基づき、本プロジェクトの技術スタックと社内ツールというコンテキストに最適化された品質目標（セキュリティ、性能、保守性など）を定義する。社内ツール・小規模チーム前提のため、9特性すべてに同じ深さで記述せず、決定事項への紐付けと「今後検討が必要な点」の明確化を優先する。

前提となるプロジェクトコンテキストは [README.md](README.md) を参照。ui-flows.md / data-models.mdとの対応は各章に記載する。

## 目次

- [1. 機能適合性](#1-機能適合性functional-suitability)
- [2. 性能効率性](#2-性能効率性performance-efficiency)
- [3. 互換性](#3-互換性compatibility)
- [4. インタラクション能力](#4-インタラクション能力interaction-capability)
- [5. 信頼性](#5-信頼性reliability)
- [6. セキュリティ](#6-セキュリティsecurity)
- [7. 保守性](#7-保守性maintainability)
- [8. 移植性](#8-移植性portability)
- [9. 安全性](#9-安全性safety)

## 1. 機能適合性（Functional Suitability）

ui-flows.md / data-models.mdで定義した機能要件を正しく満たすことを基本とし、特に以下の点に漏れがないことを品質目標とする。

- **個人情報のマスク化／非表示の漏れ防止**:
  - 検索ページからエンジニアページに遷移した際の経歴書閲覧（[ui-flows.md UC-R2](ui-flows.md#4-経歴書コンテキスト)）で、個人情報マスク（`view_personal_info` 権限判定、[APP-ADR-0007](../adr/APP-ADR-0007-rolesをpermissionベースに再定義しvisibility_rulesを廃止.md)）が確実に適用されること
  - マスク対象は経歴書に限らず、エンジニアページで表示する情報全般（星取表閲覧UC-S5等を含む）を対象として再確認する（検索結果一覧自体ではなく、エンジニアページ遷移後の表示が対象）
- **同時更新時の整合性**: 同一データへの同時更新が発生した場合、楽観ロックにより一方の更新のみを成功させ、後勝ちによる無自覚な上書きを防ぐ
- **アカウントの停止・復活**: 管理者によるアカウント停止・停止解除（UC-A7）が正しく反映されること
- **長期停止アカウントのマスク化**: 一定期間（1年）停止状態が続いたアカウントは、エンジニアページ等での表示がマスク対象となること
- **経歴書の検索条件**: 検索画面で経歴書を「案件名×言語」で検索できること（単体検索も可能）（[data-models.md 4章](data-models.md#4-経歴書コンテキスト)）
- **星取表の検索条件**: 検索画面で星取表を「言語（ツール）×レベル」で検索できること（単体検索も可能）（[data-models.md 3章](data-models.md#3-星取表コンテキスト)）

### data-models.mdへの反映状況

上記のうち、以下はデータモデルの追加検討が必要だった項目であり、すべてdata-models.mdへ反映済み。

- **楽観ロック用カラム**: 編集対象テーブル（`accounts` / `skill_master_items` / `level_master_items` / `user_skills` / `resumes`）に`version`を追加済み（[data-models.md 0章](data-models.md#0-設計方針)）
- **長期停止の判定**: `accounts.suspended_at`を追加済み。バッチ更新／動的判定のどちらにするかは実装フェーズで決定（[data-models.md 1章](data-models.md#1-認証アカウントコンテキスト)）
- **マスク対象範囲の拡張**: 個人情報マスク対象は`resumes.nearest_station`/`final_education`の2列（[APP-ADR-0007](../adr/APP-ADR-0007-rolesをpermissionベースに再定義しvisibility_rulesを廃止.md)）。将来の拡張は`roles`テーブルへの権限コード追加で対応する。
- **検索条件への対応**: 経歴書は`resumes` / `resume_projects`の正規化テーブル化、星取表は`skill_categories` / `level_categories`によるスキル×レベルの構造化により対応済み（[data-models.md 3章・4章](data-models.md#3-星取表コンテキスト)）

## 2. 性能効率性（Performance Efficiency）

社内利用（数十〜数百人規模）を前提に、一般的なWebアプリの応答性を目標とする。応答時間の目安はJakob Nielsenの研究に基づく以下の3段階を参考にする。

| 時間 | ユーザーの感じ方 |
|---|---|
| 0.1秒以内 | 「即座に反応した」と感じる |
| 1秒以内 | 多少の遅れは感じるが、思考が中断されない |
| 10秒以内 | これを超えると「待たされている」と感じる |

### 目標値

| 操作 | 目標 | 理由 |
|---|---|---|
| 検索結果一覧の表示 | 1秒以内 | 社内データ量であればPostgreSQLでの検索は十分高速に収まる |
| マイページ／エンジニアページの表示 | 1秒以内 | 同上 |
| 経歴書・星取表の登録・更新（保存） | 1秒以内 | ユーザー操作への即時フィードバックが必要 |
| 経歴書のExcel/PDF出力（[ui-flows.md UC-F1](ui-flows.md#5-ファイルコンテキスト)） | 10秒以内 | Jxls + LibreOffice変換は処理が重く、ローディング表示があれば許容範囲 |
| 星取表のレイアウト切替（区分タブ等） | 0.1秒以内 | 「すぐに反応した」と感じられる操作性が必要。表示済みデータ内でのクライアント側切替を想定し、切替都度のAPI再呼び出しは避ける |

### 補足

- 大規模なスケーラビリティ（同時アクセス数千〜数万等）はStep1の対象外とする。社内利用規模を前提とし、将来利用者数が大幅に増えた場合は別途見直す。
- 星取表のレイアウト切替を高速化するため、エンジニアページ・マイページ表示時に星取表データを一括取得し、区分（`skill_categories`/`level_categories`）ごとの切替はクライアント側（フロントエンド）の表示制御で行う想定（[data-models.md 3章](data-models.md#3-星取表コンテキスト)）。

## 3. 互換性（Compatibility）

- **API契約の維持**: フロントエンド（Nuxt3、別リポジトリ）とのインターフェースはOpenAPI定義を正とし、契約変更時はOpenAPI定義の更新とクライアント再生成をセットで行う（[docs/TODO.md](../TODO.md)の「Nuxt（TypeScript）+ OpenAPIでAPIクライアントを自動生成」方針）。
- **外部サービス連携の安定性**: Google SSO（認証）、Googleメッセージ（[ui-flows.md UC-M1](ui-flows.md#2-メッセージコンテキスト)）など、Google系サービスとの連携が継続的に動作すること。

### 補足

- **未確定事項**: キャリアシートは別システムとして検討中（[README.md 7章](README.md#7-イベントストーミング結果と業務ルール)）。Step1では本リポジトリのスコープ外だが、将来別システムと連携する場合（例: アカウント情報や星取表データの共有）は、本書の互換性目標に「キャリアシートシステムとのデータ連携」を追加する必要がある。現時点では連携方式は未定のため、項目としてのみ記録する。キャリアシート自体のフォーマット管理・動的フォーム基盤の設計メモは[docs/TODO.md「キャリアシート・ダイナミックフォーム基盤」](../TODO.md#キャリアシートダイナミックフォーム基盤)を参照。

## 4. インタラクション能力（Interaction Capability）

詳細なUI仕様・コンポーネント設計はフロントエンドプロジェクトの領域（[README.md 6章](README.md#6-フロントエンド関連ドキュメントの移行方針)で将来移行対象）のため、本書では方針レベルの目標を記載する。

- **一貫したナビゲーション**: マイページ／検索ページ・エンジニアページ／管理者画面の3系統（[ui-flows.md 0章](ui-flows.md#0-全体画面構成)）に沿った、迷いのない操作体験を提供する。
- **エラー時のわかりやすさ**: エラー発生時、ユーザーが次に取るべき行動が分かるメッセージを表示する。
- **マルチデバイス対応**: PCに加えスマートフォンでも利用できるよう、レスポンシブデザインに対応する。
- **アクセシビリティ（色覚多様性対応）**: 色弱（色覚特性）のユーザーでも情報を正しく識別できるよう、色だけに依存しない表現（アイコン・ラベル併用等）に加え、色覚特性に対応した配色モード（カラーモード切替）を提供する。

### 補足

- 上記はフロントエンド着手時に、具体的なUIライブラリ・配色設計（[docs/TODO.md](../TODO.md)の「UIライブラリの選定」）に落とし込む。本書では「対応する／対応しない」の方針確認に留める。

## 5. 信頼性（Reliability）

- **可用性**: 8:00〜24:00の間、利用可能な状態を維持する（メンテナンスは0:00〜8:00に行う）。
- **障害許容性**:
  - ログイン（認証）に障害が発生した場合は、全機能を停止してよい。
  - ログイン以外の機能（経歴書・星取表・検索・ファイル出力等）で障害が発生した場合は、当該機能のみ縮退させ、システム全体は停止しないこと。
- **回復性**: 障害発生から1日以内に復旧する。
- **データの保存**: 1週間に1度、定期的にデータベースのバックアップを取得する。

### 補足

- バックアップ頻度（週次）は初期運用での仮値であり、データ更新頻度・重要度に応じて見直す。
- 障害許容性における「機能ごとの縮退」は、[ui-flows.md 0章](ui-flows.md#0-全体画面構成)の画面構成（マイページ／検索・エンジニアページ／管理者画面）を単位とした切り分けを想定するが、実装方式（機能フラグ等）は実装フェーズで検討する。

## 6. セキュリティ（Security）

- **機密性・真正性**: Google SSO + JIT（Just-In-Time）プロビジョニングにより認証する。識別子は`accounts.google_sub_hash`（決定的ハッシュ）で照合し、平文の`sub`は保持しない（[data-models.md 1章](data-models.md#1-認証アカウントコンテキスト)）。
- **認可**: `@PreAuthorize`によるロール制御に加え、`roles` / `account_roles`（[data-models.md 1章](data-models.md#1-認証アカウントコンテキスト)）により、権限（Permission）単位で経歴書等の項目可視性を制御する（[APP-ADR-0007](../adr/APP-ADR-0007-rolesをpermissionベースに再定義しvisibility_rulesを廃止.md)）。
- **通信の暗号化**: HTTPS必須とする。
- **認証情報の管理**: Google SSOのみを利用し、本システム独自のパスワード管理は行わない。
- **責任追跡性（編集ログ）**: 経歴書・星取表等の編集操作について、「誰が・いつ・何を編集したか」を記録する。具体的には、全テーブルに登録者・更新者（`created_by` / `updated_by`、`accounts.account_id`またはバッチのリクエストID、[data-models.md 0章](data-models.md#0-設計方針)）を持たせることに加え、変更履歴を残すログ・テーブルの追加を検討する。

### data-models.mdへの反映済み事項

- **登録者・更新者（created_by / updated_by）**: 全テーブルに監査カラム（`created_at` / `updated_at` / `created_by` / `updated_by`）を付与する方針を[data-models.md 0章](data-models.md#0-設計方針)に反映済み。
- **編集履歴ログ**: 編集前後の値や編集日時を記録する汎用ログテーブル`entity_change_logs`の方針を[data-models.md 0章](data-models.md#0-設計方針)に記載済み。対象テーブル・粒度の詳細は実装フェーズで確定する。

## 7. 保守性（Maintainability）

既存の方針をそのまま品質目標として引用する。

- **モジュール性**: DDD & Clean Architectureに基づき、ドメイン層（エンティティ・値オブジェクト）をSpring/MyBatisから独立させる（[README.md 4章](README.md#4-アーキテクチャ原則adr化予定の方針)）。CQRSにより、更新系（ドメインモデル経由）と参照系（MyBatis直結）を分離する。
- **修正性・解析性**:
  - ktlint（Spotless）によるコーディング規約の統一（[README.md 3章](README.md#3-技術スタック)）。
  - レイヤーごとのカバレッジ閾値（ドメイン層80%以上等）、複雑度しきい値（CCN上限10、仮値）の運用（[docs/TODO.md](../TODO.md)）。
- **試験性**: ドメイン層をフレームワークから独立させることで、Spring/DBに依存しない単体テストを可能にする。

### 補足

- 上記はすべて既存の方針（README.md / TODO.md）の引用であり、本書で新たに追加する目標はない。MyBatis以外のO/Rマッパーへの変更等、将来の技術選定変更はADRで判断する。

## 8. 移植性（Portability）

- **適応性**: Dev Containers（VSCode）により開発環境を統一し、ローカル・CI間での差異を抑える（[README.md 3章](README.md#3-技術スタック)）。
- **デプロイ先**: クラウド環境へのデプロイを想定する。コンテナ化された開発環境を前提とすることで、クラウド側のコンテナ実行環境（具体的なサービスは未確定）への移行を容易にする。
- **データベース**: PostgreSQL（JSONB活用）を前提とし、DB製品の置き換えは想定しない。

### 補足

- **未確定事項**: クラウド事業者・具体的なホスティングサービス（コンテナ実行環境、マネージドPostgreSQL等）は未確定。確定した場合はADRに記録する。検討候補・観点は[docs/TODO.md「クラウド事業者・ホスティング選定」](../TODO.md#クラウド事業者ホスティング選定)を参照。

## 9. 安全性（Safety）

本プロジェクトでは物理的な危害は想定しないが、経歴書・星取表という個人のキャリア情報を扱うため、「情報の取り扱いによって本人に不利益・悪影響が生じないこと」を安全性の目標とする。

- **意図しない情報開示の防止**: 経歴書・星取表が、権限制御（[6章 セキュリティ](#6-セキュリティsecurity)の`view_personal_info`権限判定）に反して意図しない相手に表示されないこと。誤った情報開示は、社内での評価・人間関係に悪影響を与える可能性があるため、1章「機能適合性」のマスク化の漏れ防止と合わせて重視する。
- **誤操作による不利益の防止**: 経歴書・星取表の誤削除・誤更新が、本人のキャリア情報に重大な不利益を与えないよう、1章の楽観ロックおよび6章の編集ログ（誰が・いつ・何を編集したか）により、誤操作の検知・追跡を可能にする。

### 補足

- 本章は新たな仕組みを追加するものではなく、1章（機能適合性）・6章（セキュリティ）で定義した仕組みを「人への悪影響防止」という観点から再確認する位置づけ。
